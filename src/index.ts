export function executeInit(destinationPath: string): string {
  // TODO
  // 1) ディレクトリありますか？
  // 2) ディレクトリがないなら生成を試みる
  // 3) 生成できなかったらエラーで落とす
  // 4) src/index.md を生成する
  return destinationPath;
}
